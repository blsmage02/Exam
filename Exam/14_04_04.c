/*#include <stdio.h>

int main()
{
	int num = 0;

	while (1)				//정상적인 나이를 입력할 때가지 반복하기 위해 무한 반복
	{
		printf("input age : ");
		scanf("%d", &num);			//한 개의 정수 값 입력

		if (num > 0 && num <= 130)	//입력 받은 정수 값이 0보다 크고 130 이하인 숫자만 정상적인 나이
			break;
		else
			printf("Incorrect Age!!\n");	//나이의 범위가 잘못 입력되었다고 알림
	}

	printf("your age : %d", num);
}
*/